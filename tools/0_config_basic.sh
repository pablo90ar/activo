# Configurar raspi con sudo raspi-config
# Configurar locales con sudo dpkg-reconfigure locales
# Conectar a wifi
# Setear usuario a "kuda" y contraseña
# Conectarse por ssh
# Setear equipo a "activo" con sudo hostnamectl set-hostname activo
# Deshabilitar login con contraseña al inicio
# Actualizar dependencias con:
#   sudo apt update
#   sudo apt full-upgrade -y
#   sudo apt autoremove -y
#   sudo reboot
# Configurar locales con sudo dpkg-reconfigure locales en_GB (o es_AR para argentina)
# Instalar git con sudo apt install git
# Clonar repo con git clone https://github.com/pablo90ar/activo.git
# Ejecutar siguientes scripts
